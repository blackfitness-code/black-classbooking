// firestore.js — transaction helpers ถูกลบออกแล้ว (Phase 2 migration)
// safeBooking, safeCancelBooking → ใช้ REST API แทน (api.post/api.del)
// cachedGetDocs, invalidateCache → ไม่มีที่ไหน import ใช้อีกแล้ว
